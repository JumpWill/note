# 19. 框架对比与选型

深度学习框架是建模与训练的工具。本章对比主流框架。

## 19.1 主流框架概览

### PyTorch (Meta)

- 动态图（Eager）+ 静态优化
- 学术界主流
- 生态丰富（torchvision / torchtext / torchaudio / torch_geometric）

### TensorFlow (Google)

- 静态图 + Eager
- 工业界历史主流
- TF Lite / TF.js 部署生态

### JAX (Google)

- 函数式 + 编译
- TPU 友好
- Flax / Haiku / Optax 库

### PaddlePaddle (百度)

- 中文友好
- 工业部署成熟

### MindSpore (华为)

- 昇腾芯片原生
- 国内生态

### MXNet (Apache)

- 已被边缘化

### CNTK (Microsoft)

- 已停止维护

## 19.2 PyTorch 详细

### 优点

- ✅ Pythonic、易调试
- ✅ 动态图灵活
- ✅ 学术首选
- ✅ 生态丰富
- ✅ torch.compile (2.0+) 性能强

### 缺点

- 静态图优化较弱（vs TF）
- 分布式需手动配置
- 移动端较弱

### 核心模块

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
import torch.optim as optim
import torch.utils.data as data
from torch.utils.data import Dataset, DataLoader
```

### 子库

| 子库 | 用途 |
|------|------|
| torchvision | CV |
| torchtext | NLP |
| torchaudio | 音频 |
| torch_geometric | GNN |
| torchrec | 推荐 |
| torcharrow | 数据 |
| torchserve | 部署 |
| torchx | 分布式 |

### 生态库

- **HuggingFace Transformers**：LLM
- **timm**：视觉
- **lightning**：训练循环
- **accelerate**：分布式简化
- **peft**：参数高效微调
- **trl**：RLHF
- **diffusers**：扩散模型
- **deepspeed**：分布式优化

## 19.3 TensorFlow 详细

### 优点

- ✅ 生产部署成熟
- ✅ TF Serving
- ✅ TF Lite 移动端
- ✅ TPU 优化好

### 缺点

- ❌ API 复杂
- ❌ 学术渗透弱
- ❌ 调试难

### 核心 API

```python
import tensorflow as tf

# Sequential
model = tf.keras.Sequential([
    tf.keras.layers.Dense(256, activation='relu'),
    tf.keras.layers.Dense(10),
])

# Functional
inputs = tf.keras.Input(shape=(784,))
x = tf.keras.layers.Dense(256, activation='relu')(inputs)
outputs = tf.keras.layers.Dense(10)(x)
model = tf.keras.Model(inputs, outputs)
```

### TF Lite

```python
converter = tf.lite.TFLiteConverter.from_saved_model('model_dir')
tflite_model = converter.convert()
```

### TF.js

```javascript
// 前端推理
const model = await tf.loadLayersModel('model.json');
const pred = model.predict(input);
```

### Keras 3.0

- 多后端（PyTorch / TF / JAX）
- 统一 API

## 19.4 JAX 详细

### 思想

函数式 + 编译：

```python
import jax
import jax.numpy as jnp

# grad 自动微分
grad_fn = jax.grad(lambda x: x ** 2)
grad_fn(3.0)  # 6.0

# jit 编译
@jax.jit
def predict(params, x):
    return jnp.dot(x, params)

# vmap 自动向量化
batched_predict = jax.vmap(predict, in_axes=(None, 0))
```

### 优点

- ✅ TPU 原生
- ✅ 函数式 + 不可变
- ✅ 自动微分强大
- ✅ Google 研究用

### 缺点

- ❌ 生态较新
- ❌ 静态图（不易调试）
- ❌ 学习曲线陡

### 生态

- **Flax**：神经网络
- **Haiku**：DeepMind
- **Optax**：优化器
- **Equinox**：PyTorch 风格
- **Distrax**：概率分布
- **RLlib**：RL
- **MaxText**：LLM 训练

## 19.5 PaddlePaddle 详细

### 优点

- ✅ 中文文档完善
- ✅ 工业部署好
- ✅ 大模型套件（PaddleFleet）

### PaddleNLP / PaddleCV / PaddleSpeech

类似 transformers / torchvision。

### 大模型

```python
from paddlenlp.transformers import LlamaForCausalLM

model = LlamaForCausalLM.from_pretrained("meta-llama/Llama-2-7b")
```

### 国产芯片支持

- 昆仑芯
- 海光 DCU
- 寒武纪 MLU

## 19.6 MindSpore 详细

### 优点

- ✅ 昇腾 NPU 原生
- ✅ 自动并行
- ✅ 图算融合

### 缺点

- ❌ 生态较新
- ❌ 学术界渗透弱

### 框架

```python
import mindspore as ms
from mindspore import nn, ops

class Net(nn.Cell):
    def __init__(self):
        super().__init__()
        self.fc1 = nn.Dense(784, 256)
        self.fc2 = nn.Dense(256, 10)

    def construct(self, x):
        x = ops.relu(self.fc1(x))
        return self.fc2(x)
```

## 19.7 对比总览

| 维度 | PyTorch | TF | JAX | Paddle | MindSpore |
|------|---------|----|----|--------|-----------|
| 学术 | **首选** | 中 | 中 | 中 | 弱 |
| 工业 | 强 | **强** | 中 | 中 | 弱 |
| 部署 | 中 | **强** | 中 | 中 | 中 |
| 移动端 | 弱 | **强** | 弱 | 中 | 弱 |
| TPU | 中 | **强** | **强** | 弱 | 弱 |
| 中文 | 中 | 中 | 弱 | **强** | 中 |
| 学习曲线 | 平缓 | 中 | 陡 | 平缓 | 中 |
| 动态图 | ✅ | ✅ | ❌ (jit) | ✅ | ✅ |
| 静态图 | torch.compile | ✅ | ✅ | ✅ | ✅ |

## 19.8 选型决策

### 按场景

| 场景 | 推荐 |
|------|------|
| 学术研究 | **PyTorch** |
| 视觉 SOTA | PyTorch + timm |
| LLM 研究 | PyTorch + transformers |
| 工业部署 | TF / PyTorch + TorchServe |
| 移动端 | **TF Lite** / PyTorch Mobile |
| Web 端 | TF.js |
| 中文 NLP | PyTorch + HuggingFace |
| TPU | **JAX / TF** |
| 国产芯片 | **MindSpore / Paddle** |
| 快速实验 | PyTorch Lightning |

### 按团队

| 团队类型 | 推荐 |
|----------|------|
| 研究团队 | PyTorch |
| 产品团队 | TF / PyTorch |
| 算法工程师 | PyTorch |
| 嵌入式 | TF Lite / ONNX |

## 19.9 ONNX：跨框架

### 导出

```python
# PyTorch → ONNX
torch.onnx.export(model, x, "model.onnx")

# TF → ONNX
# tf2onnx

# JAX → ONNX
# jax2onnx
```

### 推理

```python
import onnxruntime as ort

session = ort.InferenceSession("model.onnx")
result = session.run(None, {"input": x.numpy()})
```

### 优势

- 跨框架
- 优化推理
- 多语言支持

## 19.10 训练辅助库

### Hugging Face Transformers

LLM 标准：

```python
from transformers import AutoModel, AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("bert-base-uncased")
model = AutoModel.from_pretrained("bert-base-uncased")
```

### timm

视觉 SOTA 集合：

```python
import timm

model = timm.create_model("resnet50", pretrained=True)
model = timm.create_model("vit_base_patch16_224", pretrained=True)
```

### PyTorch Lightning

简化训练循环：

```python
import pytorch_lightning as pl

class MyModel(pl.LightningModule):
    def __init__(self):
        super().__init__()
        self.model = nn.Linear(10, 1)

    def training_step(self, batch, batch_idx):
        x, y = batch
        loss = F.mse_loss(self.model(x), y)
        return loss

    def configure_optimizers(self):
        return torch.optim.Adam(self.parameters(), lr=1e-3)
```

### HuggingFace Accelerate

简化分布式：

```python
from accelerate import Accelerator

accelerator = Accelerator()
model, optimizer, dataloader = accelerator.prepare(model, optimizer, dataloader)
```

### FastAI

高层封装：

```python
from fastai.vision.all import *

learn = cnn_learner(dls, resnet50, metrics=accuracy)
learn.fine_tune(5)
```

## 19.11 实验管理

### Weights & Biases

```python
import wandb
wandb.init(project='my-project')
wandb.log({'loss': loss, 'acc': acc})
wandb.watch(model)
```

### MLflow

```python
import mlflow
mlflow.start_run()
mlflow.log_param("lr", 1e-3)
mlflow.log_metric("loss", loss)
mlflow.pytorch.log_model(model, "model")
```

### TensorBoard

```python
from torch.utils.tensorboard import SummaryWriter
writer = SummaryWriter()
writer.add_scalar('loss', loss, epoch)
```

### ClearML

自动化实验管理。

### DVC

数据 + 模型版本控制。

## 19.12 部署框架

### TorchServe

```bash
torchserve --start --model-store model_store --models my_model=model.mar
```

### TF Serving

```bash
tensorflow_model_server \
    --model_base_path=/models \
    --rest_api_port=8501
```

### Triton Inference Server

```bash
tritonserver --model-repository=/models
```

### BentoML

ML 模型打包 + 服务化。

## 19.13 学习路径

### 初学者

1. PyTorch (Karpathy 视频)
2. timm (视觉)
3. Transformers (LLM)

### 中级

1. torch.compile 优化
2. Lightning / Accelerate
3. DeepSpeed / FSDP

### 高级

1. JAX + Flax (TPU 研究)
2. 自定义算子 (CUDA)
3. 框架内部 (编译优化)

## 19.14 国产芯片 + 框架

| 芯片 | 推荐框架 |
|------|----------|
| 昇腾 NPU | **MindSpore** |
| 昆仑芯 XPU | Paddle / PyTorch |
| 海光 DCU | PyTorch (DTK) |
| 寒武纪 MLU | Paddle / PyTorch |
| 摩尔线程 MTT | PyTorch (MUSA) |

## 19.15 推理优化框架

| 框架 | 特点 |
|------|------|
| ONNX Runtime | 跨平台、CPU 友好 |
| TensorRT | NVIDIA GPU |
| OpenVINO | Intel CPU/GPU/VPU |
| TVM | 多硬件编译 |
| llama.cpp | CPU/GPU LLM |
| vLLM | GPU LLM 服务 |
| TensorRT-LLM | NVIDIA LLM |
| MNN | 移动端 |

## 19.16 参考

- Paszke et al., 2019 *PyTorch: An Imperative Style, High-Performance Deep Learning Library*
- Abadi et al., 2016 *TensorFlow: A System for Large-Scale Machine Learning*
- Bradbury et al., 2018 *JAX: Composable Transformations of Python+NumPy Programs*
- Howard & Gugger, 2020 *Deep Learning for Coders with fastai & PyTorch*
- 官方文档：pytorch.org、tensorflow.org、jax.readthedocs.io