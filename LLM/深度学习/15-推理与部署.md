# 15. 推理与部署

训练完成后，模型需要部署到生产环境。本章覆盖量化、优化、服务化等。

## 15.1 推理 vs 训练

| 维度 | 训练 | 推理 |
|------|------|------|
| 显存 | **大** | 可压缩 |
| 速度 | 不急 | 关键 |
| 精度 | 高 | 可适当低 |
| 批大小 | 大 | 可能 = 1 |
| 在线 | 否 | 必 |

## 15.2 量化 (Quantization)

### 思想

低 bit 表示权重/激活：

- FP32 → FP16 (50% 减)
- FP32 → INT8 (75% 减)
- FP32 → INT4 (87.5% 减)
- FP32 → FP8 (75% 减)

### 类型

#### 训练后量化 (PTQ)

训练完成后量化：

```python
from torch.ao.quantization import quantize_dynamic

model = AutoModelForCausalLM.from_pretrained(...)
quantized_model = quantize_dynamic(
    model, {nn.Linear}, dtype=torch.qint8
)
```

**优点**：简单
**缺点**：精度损失较大

#### 量化感知训练 (QAT)

训练中模拟量化：

```python
model.qconfig = torch.ao.quantization.get_default_qat_qconfig('qnnpack')
torch.ao.quantization.prepare_qat(model, inplace=True)
# 训练 ...
torch.ao.quantization.convert(model, inplace=True)
```

精度更好，但慢。

### LLM 量化方法

#### GPTQ

- 单层量化 + 误差补偿
- GPTQ-for-LLaMa / AutoGPTQ

```bash
pip install auto-gptq
```

```python
from auto_gptq import AutoGPTQForCausalLM, BaseQuantizeConfig

model = AutoGPTQForCausalLM.from_pretrained(
    model_name,
    quantize_config=BaseQuantizeConfig(bits=4, group_size=128),
)
model.quantize(examples)
model.save_pretrained("gptq-model")
```

#### AWQ (Activation-aware Weight Quantization)

- 保护重要权重（1%）
- 4-bit SOTA

```bash
pip install autoawq
```

#### BitsandBytes (QLoRA)

```python
from transformers import BitsAndBytesConfig

bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16,
    bnb_4bit_use_double_quant=True,
)
model = AutoModelForCausalLM.from_pretrained(
    model_id, quantization_config=bnb_config,
)
```

#### GGUF / llama.cpp

CPU/GPU 推理：

```bash
# 转换模型
python convert.py model_dir --outfile model.gguf
# 量化
./quantize model.gguf model-q4.gguf q4_0
# 推理
./main -m model-q4.gguf -p "Hello"
```

#### SmoothQuant

激活量化 W8A8：

```python
# smoothquant pip
```

### 量化精度对比

| 方法 | 精度 | 显存 (7B) | 速度 |
|------|------|-----------|------|
| FP16 | 100% | 14GB | 1x |
| INT8 | 99% | 7GB | 1.5x |
| INT4 (GPTQ) | 97% | 4GB | 2x |
| INT4 (AWQ) | 98% | 4GB | 2.5x |
| INT3 | 92% | 3GB | 3x |
| INT2 | 70-85% | 2GB | 4x |

## 15.3 模型优化

### 剪枝 (Pruning)

#### 非结构化剪枝

```python
import torch.nn.utils.prune as prune

prune.l1_unstructured(layer, 'weight', amount=0.5)
```

#### 结构化剪枝

整头 / 整通道剪枝。

#### SparseGPT

LLM 一次性剪枝 50%。

### 知识蒸馏 (Distillation)

```python
def distillation_loss(student_logits, teacher_logits, labels, T=2.0, alpha=0.5):
    soft = F.kl_div(
        F.log_softmax(student_logits / T, dim=-1),
        F.softmax(teacher_logits / T, dim=-1),
        reduction='batchmean',
    ) * (T * T)
    hard = F.cross_entropy(student_logits, labels)
    return alpha * soft + (1 - alpha) * hard
```

### 模型合并 (Merging)

```python
# slerp 线性插值
from transformers import pipeline

model_a = AutoModelForCausalLM.from_pretrained("model_a")
model_b = AutoModelForCausalLM.from_pretrained("model_b")

# 加权平均
for p_a, p_b in zip(model_a.parameters(), model_b.parameters()):
    p_a.data = 0.7 * p_a.data + 0.3 * p_b.data
```

## 15.4 高效 Attention

### Flash Attention 2/3

```python
from torch.nn.functional import scaled_dot_product_attention

output = scaled_dot_product_attention(
    Q, K, V, is_causal=True,
)
```

- IO 优化
- 速度 2-4x
- 显存节省

### Paged Attention (vLLM)

- KV cache 分页管理
- 显存高效

### Multi-Query / Grouped Query

- 共享 KV
- 减少 KV cache

## 15.5 KV Cache 管理

### 基本

```python
class KVCache:
    def __init__(self):
        self.k = None
        self.v = None
        self.length = 0

    def update(self, k_new, v_new):
        if self.k is None:
            self.k = k_new
            self.v = v_new
        else:
            self.k = torch.cat([self.k, k_new], dim=2)
            self.v = torch.cat([self.v, v_new], dim=2)
        self.length += k_new.size(2)
        return self.k, self.v
```

### Paged Attention

vLLM / TGI 用的方法：

- KV cache 分页（类似 OS 虚拟内存）
- 显存高效
- 连续 batching

## 15.6 推理框架

### vLLM

```python
from vllm import LLM, SamplingParams

llm = LLM(model="meta-llama/Llama-3-8B-Instruct")
params = SamplingParams(temperature=0.8, max_tokens=100)

output = llm.generate(["Hello, how are"], params)
```

特点：
- Paged Attention
- 连续批处理
- 高吞吐

### TGI (Text Generation Inference)

```bash
docker run -p 8080:80 \
  -v $HOME/models:/models \
  ghcr.io/huggingface/text-generation-inference:latest \
  --model-id meta-llama/Llama-3-8B-Instruct
```

### TensorRT-LLM

NVIDIA 高性能推理：

- CUDA kernel 优化
- In-flight batching
- 多 GPU

```bash
trtllm-build --model_dir ./llama \
    --output_dir ./engine \
    --gemm_plugin float16
```

### llama.cpp / GGUF

CPU 推理：

```bash
# 下载 GGUF 模型
# 量化
./quantize model.gguf model-q4.gguf q4_0
# 服务
./server -m model-q4.gguf -c 4096
```

### SGLang

```python
import sglang as sgl

runtime = sgl.Runtime(model_path="meta-llama/Llama-3-8B-Instruct")
runtime.generate(prompt="Hello")
```

### Ollama

本地 LLM 运行：

```bash
ollama run llama3
```

### LM Studio

本地 GUI。

## 15.7 ONNX 导出

```python
import torch.onnx

torch.onnx.export(
    model,
    dummy_input,
    "model.onnx",
    opset_version=14,
    input_names=['input'],
    output_names=['output'],
    dynamic_axes={'input': {0: 'batch'}, 'output': {0: 'batch'}},
)
```

### ONNX Runtime

```python
import onnxruntime as ort

session = ort.InferenceSession("model.onnx")
result = session.run(None, {"input": input_data.numpy()})
```

## 15.8 TorchScript / TorchCompile

```python
# TorchScript
scripted = torch.jit.script(model)
scripted.save("model.pt")

# TorchCompile (PyTorch 2.0+)
compiled = torch.compile(model)
```

## 15.9 服务化

### FastAPI

```python
from fastapi import FastAPI, Request
import torch

app = FastAPI()
model = AutoModelForCausalLM.from_pretrained("model_dir", torch_dtype=torch.bfloat16).cuda()
tokenizer = AutoTokenizer.from_pretrained("model_dir")

@app.post("/generate")
async def generate(req: Request):
    data = await req.json()
    prompt = data["prompt"]
    inputs = tokenizer(prompt, return_tensors="pt").to("cuda")
    outputs = model.generate(**inputs, max_new_tokens=100)
    return {"text": tokenizer.decode(outputs[0])}
```

### gRPC

```python
import grpc
from concurrent import futures

# 定义 proto, 实现服务
```

### 流式响应 (SSE)

```python
from fastapi.responses import StreamingResponse

@app.post("/stream")
async def stream(req: Request):
    async def event_generator():
        for token in generate_stream(...):
            yield f"data: {token}\n\n"
    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

## 15.10 部署模式

### 单 GPU

- 模型加载后常驻
- 请求队列

### 多 GPU

- 模型并行 / 流水线并行
- 请求分片

### 多节点

- 推理集群
- 负载均衡

### 边缘 / 端侧

- TFLite / Core ML / ONNX Runtime Mobile
- llama.cpp / MNN
- 量化到 INT4 甚至 INT2

### 云端

- SageMaker / Vertex AI / Azure ML
- 自托管 vLLM / TGI

## 15.11 性能优化

### Batching

- **Static Batching**：等批再推理
- **Continuous Batching**：vLLM 边生成边接受新请求
- **In-flight Batching**：TRT-LLM

### 推测解码 (Speculative Decoding)

小模型先草拟，大模型验证：

```python
# Draft model 预测 K 个 token
draft_tokens = draft_model.generate(prompt, max_tokens=K)
# 验证
verified = main_model.verify(prompt + draft_tokens)
```

加速 2-3x。

### Prefix Caching

相同前缀共享 KV cache：

- System prompt
- 长文档 RAG

### Continuous / Dynamic Batching

```python
# vLLM 自动
```

## 15.12 监控

### 关键指标

- **P50/P99 延迟**
- **QPS / RPM**
- **首 token 延迟** (TTFT)
- **token/s 生成速度**
- **GPU 利用率**
- **显存峰值**

### 工具

- Prometheus + Grafana
- Langfuse (LLM 专用)
- Helicone (LLM 监控)

## 15.13 端到端示例

```python
# === 训练后量化 ===
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig

bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_compute_dtype=torch.bfloat16,
)

model = AutoModelForCausalLM.from_pretrained(
    "meta-llama/Llama-3.1-8B-Instruct",
    quantization_config=bnb_config,
    device_map="auto",
)
tokenizer = AutoTokenizer.from_pretrained("meta-llama/Llama-3.1-8B-Instruct")

# === 推理 ===
prompt = "解释深度学习"
inputs = tokenizer(prompt, return_tensors="pt").to("cuda")

with torch.no_grad():
    outputs = model.generate(
        **inputs,
        max_new_tokens=200,
        temperature=0.7,
        top_p=0.9,
        do_sample=True,
    )

print(tokenizer.decode(outputs[0], skip_special_tokens=True))
```

## 15.14 移动端 / 嵌入式

### iOS

- Core ML
- llama.cpp for iOS

### Android

- TFLite
- llama.cpp for Android

### Web

- Transformers.js / ONNX Runtime Web

### 边缘硬件

- Apple Neural Engine
- Qualcomm NPU
- Jetson Orin

## 15.15 安全 / 成本

### 安全

- 输入验证
- 输出过滤
- 防止 prompt injection
- 速率限制

### 成本优化

- 量化减少 GPU
- 缓存
- 异步
- 请求路由（小模型/大模型）

## 15.16 参考

- Jacob et al., 2018 *Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference*
- Frantar et al., 2022 *GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers*
- Lin et al., 2023 *AWQ: Activation-aware Weight Quantization*
- Dettmers et al., 2022 *LLM.int8()*
- Kwon et al., 2023 *Efficient Memory Management for Large Language Model Serving with PagedAttention* (vLLM)
- Pope et al., 2023 *Efficiently Scaling Transformer Inference* (TGI)
- NVIDIA, 2024 *TensorRT-LLM*
- Dao et al., 2023 *FlashAttention-2*