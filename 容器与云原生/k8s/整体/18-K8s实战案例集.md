# K8s 实战案例集 (Real-World Case Studies)

> 本章通过 12 个真实场景的完整案例,展示 K8s 在生产中的典型应用。

## 一、案例总览

| # | 案例 | 行业 | 核心亮点 |
|---|------|------|---------|
| 1 | 从零搭建生产 K8s 集群 | 通用 | kubeadm + 外部 etcd |
| 2 | 微服务应用完整部署 | SaaS | 多服务 + Ingress + RBAC |
| 3 | 数据库部署 (MySQL) | 通用 | StatefulSet + PVC |
| 4 | CI/CD 集成 (GitLab) | DevOps | GitOps 完整流程 |
| 5 | 日志聚合 (EFK/Loki) | 通用 | 完整日志方案 |
| 6 | Prometheus + Grafana 监控 | 通用 | 完整监控体系 |
| 7 | 自动扩缩容 (HPA + VPA + CA) | 通用 | 三层自动扩缩 |
| 8 | 蓝绿发布与灰度 | 通用 | 完整发布策略 |
| 9 | 多集群管理 | 企业 | Rancher / Cluster API |
| 10 | GitOps (ArgoCD/Flux) | DevOps | 现代部署方式 |
| 11 | Service Mesh 集成 (Istio) | 微服务 | 服务网格实战 |
| 12 | 跨云灾备 | 企业 | 多云容灾 |

---

## 二、案例 1:从零搭建生产 K8s 集群

### 完整步骤

```bash
# ============ 环境准备 (所有节点) ============

# 1. 主机名与 hosts
hostnamectl set-hostname master-1
cat >> /etc/hosts << EOF
10.0.0.11 master-1
10.0.0.12 master-2
10.0.0.13 master-3
10.0.0.21 worker-1
10.0.0.22 worker-2
10.0.0.23 worker-3
EOF

# 2. 关闭 swap
swapoff -a
sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab

# 3. 内核模块
cat > /etc/modules-load.d/k8s.conf << EOF
overlay
br_netfilter
EOF
modprobe overlay
modprobe br_netfilter

# 4. sysctl
cat > /etc/sysctl.d/k8s.conf << EOF
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward = 1
EOF
sysctl --system

# 5. 关闭防火墙 (生产建议精细化规则)
systemctl stop firewalld
systemctl disable firewalld

# 6. 安装 containerd
yum install -y yum-utils device-mapper-persistent-data lvm2
yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
yum install -y containerd.io
mkdir -p /etc/containerd
containerd config default > /etc/containerd/config.toml
sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
systemctl restart containerd

# 7. 安装 kubeadm/kubelet/kubectl
cat > /etc/yum.repos.d/kubernetes.repo << EOF
[kubernetes]
name=Kubernetes
baseurl=https://pkgs.k8s.io/core:/stable:/v1.30/rpm/
enabled=1
gpgcheck=1
gpgkey=https://pkgs.k8s.io/core:/stable:/v1.30/rpm/repodata/repomd.xml.key
EOF
yum install -y kubelet kubeadm kubectl --disableexcludes=kubernetes
systemctl enable kubelet

# ============ Master 1 初始化 ============

# 8. 初始化
kubeadm init \
  --control-plane-endpoint=10.0.0.10:6443 \
  --upload-certs \
  --pod-network-cidr=192.168.0.0/16 \
  --service-cidr=10.96.0.0/12

# 9. 配置 kubectl
mkdir -p $HOME/.kube
cp -f /etc/kubernetes/admin.conf $HOME/.kube/config

# ============ 安装 CNI (Calico) ============

# 10. 安装 Calico
kubectl apply -f https://docs.projectcalico.org/manifests/calico.yaml

# 11. 验证
kubectl get nodes
kubectl get pods -n kube-system

# ============ 其他 Master 加入 ============

# 12. 在 master-2 和 master-3 执行
kubeadm join 10.0.0.10:6443 \
  --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash> \
  --control-plane \
  --certificate-key <key>

# ============ Worker 加入 ============

# 13. 在 worker 节点执行
kubeadm join 10.0.0.10:6443 \
  --token <token> \
  --discovery-token-ca-cert-hash sha256:<hash>

# 14. 验证
kubectl get nodes
# 期望: 3 个 master (Ready) + 3 个 worker (Ready)
```

### 完整生产配置

```yaml
# /etc/kubernetes/manifests/kube-apiserver.yaml 关键参数
spec:
  containers:
  - command:
    - kube-apiserver
    - --advertise-address=10.0.0.11
    - --etcd-servers=https://10.0.0.11:2379,https://10.0.0.12:2379,https://10.0.0.13:2379
    - --secure-port=6443
    - --service-cluster-ip-range=10.96.0.0/12
    - --client-ca-file=/etc/kubernetes/pki/ca.crt
    - --tls-cert-file=/etc/kubernetes/pki/apiserver.crt
    - --tls-private-key-file=/etc/kubernetes/pki/apiserver.key
    - --service-account-key-file=/etc/kubernetes/pki/sa.pub
    - --service-account-signing-key-file=/etc/kubernetes/pki/sa.key
    - --authorization-mode=Node,RBAC
    - --enable-bootstrap-token-auth=true
    - --enable-admission-plugins=NodeRestriction,PodSecurity,ServiceAccount
    - --audit-policy-file=/etc/kubernetes/audit-policy.yaml
    - --audit-log-path=/var/log/kubernetes/audit/audit.log
    - --audit-log-maxage=30
    - --encryption-provider-config=/etc/kubernetes/encryption-config.yaml
```

---

## 三、案例 2:微服务应用完整部署

### 架构

```text
外部流量
   ↓
[Nginx Ingress Controller]
   ↓
   ├─ /api/users/* → user-service
   ├─ /api/orders/* → order-service
   ├─ /api/products/* → product-service
   └─ / → frontend (Vue/React)
```

### 完整部署

```bash
# 1. 创建 namespace
kubectl create ns ecommerce
kubectl label ns ecommerce env=production

# 2. 创建 Secret
kubectl create secret generic db-secret -n ecommerce \
  --from-literal=DB_PASSWORD='S3cr3t!'

# 3. 创建 ConfigMap
kubectl apply -f -n ecommerce -f - << EOF
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ecommerce
data:
  LOG_LEVEL: "INFO"
  CACHE_TTL: "300"
EOF

# 4. 部署 user-service
cat << EOF | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: ecommerce
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: myregistry/user-service:1.0.0
        ports:
        - containerPort: 8080
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_PASSWORD
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LOG_LEVEL
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ecommerce
spec:
  selector:
    app: user-service
  ports:
  - port: 80
    targetPort: 8080
EOF

# 5. 同理部署 order-service / product-service

# 6. 创建 Ingress
cat << EOF | kubectl apply -f -
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce
  namespace: ecommerce
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /\$2
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - api.example.com
    secretName: api-tls
  rules:
  - host: api.example.com
    http:
      paths:
      - path: /api/users(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: user-service
            port: { number: 80 }
      - path: /api/orders(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: order-service
            port: { number: 80 }
      - path: /api/products(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: product-service
            port: { number: 80 }
EOF

# 7. 配置 HPA
kubectl autoscale deployment user-service -n ecommerce \
  --min=3 --max=10 --cpu-percent=70
```

---

## 四、案例 3:有状态应用 (MySQL StatefulSet)

### 部署

```yaml
# 1. Headless Service
apiVersion: v1
kind: Service
metadata:
  name: mysql-headless
  namespace: database
spec:
  clusterIP: None
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306

---
# 2. StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  namespace: database
spec:
  serviceName: mysql-headless
  replicas: 3
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        readinessProbe:
          exec:
            command: ["mysqladmin", "ping"]
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 2000m
            memory: 4Gi
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: fast-ssd
      resources:
        requests:
          storage: 100Gi
```

### 应用访问 MySQL

```yaml
# 应用中通过 DNS 访问
# mysql-0.mysql-headless.database.svc.cluster.local
# mysql-1.mysql-headless.database.svc.cluster.local
# mysql-2.mysql-headless.database.svc.cluster.local

env:
- name: DB_HOST
  value: "mysql-0.mysql-headless.database"
- name: DB_REPLICA_HOST
  value: "mysql-1.mysql-headless.database,mysql-2.mysql-headless.database"
```

---

## 五、案例 4:CI/CD 集成

### GitLab CI/CD + K8s 部署

```yaml
# .gitlab-ci.yml
stages:
- build
- test
- deploy

variables:
  DOCKER_REGISTRY: registry.example.com
  APP_NAME: my-app
  K8S_NAMESPACE: production

build:
  stage: build
  script:
    - docker build -t ${DOCKER_REGISTRY}/${APP_NAME}:${CI_COMMIT_SHORT_SHA} .
    - docker push ${DOCKER_REGISTRY}/${APP_NAME}:${CI_COMMIT_SHORT_SHA}

test:
  stage: test
  script:
    - docker run --rm ${DOCKER_REGISTRY}/${APP_NAME}:${CI_COMMIT_SHORT_SHA} npm test

deploy:production:
  stage: deploy
  image: bitnami/kubectl
  script:
    - kubectl config use-context production
    - kubectl set image deployment/${APP_NAME}
        ${APP_NAME}=${DOCKER_REGISTRY}/${APP_NAME}:${CI_COMMIT_SHORT_SHA}
        -n ${K8S_NAMESPACE}
    - kubectl rollout status deployment/${APP_NAME} -n ${K8S_NAMESPACE}
    - kubectl get pods -l app=${APP_NAME} -n ${K8S_NAMESPACE}
  environment:
    name: production
  only:
  - main
```

### Jenkins + K8s 部署

```groovy
// Jenkinsfile
pipeline {
    agent any

    environment {
        DOCKER_REGISTRY = 'registry.example.com'
        APP_NAME = 'my-app'
        K8S_NAMESPACE = 'production'
    }

    stages {
        stage('Build') {
            steps {
                sh "docker build -t ${DOCKER_REGISTRY}/${APP_NAME}:${BUILD_NUMBER} ."
                sh "docker push ${DOCKER_REGISTRY}/${APP_NAME}:${BUILD_NUMBER}"
            }
        }

        stage('Deploy') {
            steps {
                sh "kubectl set image deployment/${APP_NAME} ${APP_NAME}=${DOCKER_REGISTRY}/${APP_NAME}:${BUILD_NUMBER} -n ${K8S_NAMESPACE}"
                sh "kubectl rollout status deployment/${APP_NAME} -n ${K8S_NAMESPACE}"
            }
        }
    }
}
```

---

## 六、案例 5:日志聚合 (Loki)

### 部署 Loki + Promtail + Grafana

```bash
# 1. 添加 helm repo
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update

# 2. 安装 Loki
helm install loki grafana/loki-stack \
  --namespace logging --create-namespace \
  --set promtail.enabled=true \
  --set loki.persistence.enabled=true \
  --set loki.persistence.size=50Gi

# 3. 在 Grafana 添加 Loki 数据源
# URL: http://loki.logging:3100
```

### 应用日志配置 (Logback)

```xml
<!-- logback-spring.xml -->
<configuration>
  <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
    <http>
      <url>http://loki.logging:3100/loki/api/v1/push</url>
    </http>
    <labels>
      <label>app=my-app</label>
      <label>env=production</label>
      <label>version>1.0.0</label>
    </labels>
  </appender>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="STDOUT" />
    <appender-ref ref="LOKI" />
  </root>
</configuration>
```

### 在 Grafana 查询

```logql
# 错误日志
{app="my-app"} |= "ERROR"

# 按 namespace 分组
sum(count_over_time({namespace="production"}[5m])) by (app)

# 错误率
sum(rate({app="my-app"} |= "ERROR" [5m])) / sum(rate({app="my-app"}[5m]))
```

---

## 七、案例 6:Prometheus + Grafana 监控

### 完整部署

```bash
# 1. 安装 kube-prometheus-stack
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm install prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring --create-namespace \
  --set prometheus.prometheusSpec.retention=30d \
  --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.resources.requests.storage=50Gi \
  --set grafana.adminPassword=admin
```

### 自定义告警规则

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: app-alerts
  namespace: monitoring
spec:
  groups:
  - name: app
    rules:
    - alert: HighErrorRate
      expr: |
        sum(rate(http_requests_total{status=~"5.."}[5m])) by (service)
        / sum(rate(http_requests_total[5m])) by (service) > 0.05
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "High error rate on {{ $labels.service }}"

    - alert: HighLatency
      expr: |
        histogram_quantile(0.99,
          sum(rate(http_request_duration_seconds_bucket[5m])) by (service, le)
        ) > 1
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "P99 latency > 1s on {{ $labels.service }}"
```

---

## 八、案例 7:三层自动扩缩容

### HPA + VPA + Cluster Autoscaler

```yaml
# 1. HPA (Pod 水平扩缩)
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
  minReplicas: 3
  maxReplicas: 50
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
    scaleDown:
      stabilizationWindowSeconds: 300

---
# 2. VPA (Pod 垂直扩缩, 推荐用于 StatefulSet)
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: web-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
  updatePolicy:
    updateMode: "Auto"           # 自动调整 requests/limits

---
# 3. Cluster Autoscaler (集群节点扩缩)
# 部署 cluster-autoscaler (云厂商特定)
# AWS:
helm install cluster-autoscaler autoscaler/cluster-autoscaler \
  --set autoDiscovery.clusterName=my-cluster \
  --set aws.region=us-east-1 \
  --set extraArgs.expander=least-waste

# Min 3 节点, Max 10 节点
```

---

## 九、案例 8:蓝绿发布与灰度

### 蓝绿发布

```yaml
# 蓝版本 (当前生产)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
      version: blue
  template:
    metadata:
      labels:
        app: web
        version: blue
    spec:
      containers:
      - name: web
        image: myapp:1.0

---
# 绿版本 (新版本)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
      version: green
  template:
    metadata:
      labels:
        app: web
        version: green
    spec:
      containers:
      - name: web
        image: myapp:2.0

---
# Service 通过 selector 切换 (改 version)
apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  selector:
    app: web
    version: blue   # ← 改成 green 即切换
  ports:
  - port: 80
    targetPort: 8080
```

### 金丝雀 (Canary) - Nginx Ingress

```yaml
# 主版本 95% 流量
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-main
  annotations:
    nginx.ingress.kubernetes.io/canary-weight: "95"
spec:
  rules:
  - host: app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-v1
            port:
              number: 80

---
# 新版本 5% 流量
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-canary
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "5"
spec:
  rules:
  - host: app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-v2
            port:
              number: 80
```

---

## 十、案例 9:多集群管理 (Rancher)

### Rancher 部署

```bash
# 1. 安装 Rancher
docker run -d --restart=unless-stopped \
  -p 80:80 -p 443:443 \
  -v /opt/rancher:/var/lib/rancher \
  rancher/rancher:latest

# 2. 访问 https://<server-ip>, 完成初始化

# 3. 创建/导入集群
#    - Custom: Rancher 用 RKE 创建集群
#    - Imported: 导入现有 K8s 集群

# 4. 通过 Rancher 管理多集群
#    - 统一 Dashboard
#    - 统一 RBAC
#    - 统一监控
#    - 应用商店 (Helm)
```

---

## 十一、案例 10:GitOps (ArgoCD)

### ArgoCD 部署

```bash
# 1. 安装 ArgoCD
kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

# 2. 暴露 ArgoCD Dashboard (NodePort)
kubectl patch svc argocd-server -n argocd -p '{"spec":{"type":"NodePort"}}'
```

### GitOps 工作流

```yaml
# 1. 在 Git 仓库组织 manifests
# git-repo/
# ├── apps/
# │   ├── my-app/
# │   │   ├── namespace.yaml
# │   │   ├── deployment.yaml
# │   │   ├── service.yaml
# │   │   └── ingress.yaml
# ├── projects/
# │   └── production.yaml
# └── apps.yaml

# 2. ArgoCD Application
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
  namespace: argocd
spec:
  project: production
  source:
    repoURL: https://github.com/myorg/k8s-manifests.git
    targetRevision: main
    path: apps/my-app
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
```

### GitOps 工作方式

```text
1. 开发者提交代码到 Git (含 K8s manifests)
   ↓
2. ArgoCD 监听 Git 仓库变化
   ↓
3. 检测到 manifests 变化,自动同步到集群
   ↓
4. 集群应用更新
   ↓
5. ArgoCD Dashboard 显示同步状态
```

---

## 十二、案例 11:Service Mesh (Istio)

### Istio 部署

```bash
# 1. 下载 Istio
curl -L https://istio.io/downloadIstio | sh -
cd istio-1.21.0

# 2. 安装
export PATH=$PWD/bin:$PATH
istioctl install --set profile=demo -y

# 3. 启用 Sidecar 自动注入
kubectl label namespace default istio-injection=enabled
```

### 流量管理 (VirtualService)

```yaml
# 90% 流量到 v1, 10% 到 v2 (金丝雀)
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: web-vs
spec:
  hosts:
  - web
  http:
  - match:
    - headers:
        canary:
          exact: "true"
    route:
    - destination:
        host: web
        subset: v2
  - route:
    - destination:
        host: web
        subset: v1
      weight: 90
    - destination:
        host: web
        subset: v2
      weight: 10
```

### DestinationRule

```yaml
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: web-dr
spec:
  host: web
  subsets:
  - name: v1
    labels:
      version: v1
  - name: v2
    labels:
      version: v2
```

---

## 十三、案例 12:跨云灾备

### 架构

```text
Primary Cluster (Region A)
├── Application
├── Database (主)
└── 持续同步到
      ↓
DR Cluster (Region B)
├── Application (待命)
├── Database (备)
└── 灾备切换时激活
```

### 数据同步工具

```bash
# 1. 数据库同步 (MySQL)
#    - AWS DMS / 阿里云 DTS
#    - binlog 同步

# 2. 应用镜像同步
#    - 镜像推送到两个 region 的 registry
#    - 或使用同一 registry (Harbor)

# 3. K8s 配置同步
#    - Git 仓库为权威源
#    - ArgoCD 部署到两个集群

# 4. 灾备演练
#    - 关闭主集群模拟故障
#    - DNS 切到 DR 集群
#    - 验证服务可用
```

### 切换流程

```bash
# 1. 主集群故障检测 (Prometheus Alertmanager)
# 2. 运维确认
# 3. DNS 切换 (Route 53 / CloudDNS)
# 4. DR 集群提升
# 5. 数据库从备切到主
# 6. 服务验证

# RTO: 5-10 分钟
# RPO: 0 (同步复制) 或秒级 (异步)
```

---

## 核心要点速记

### K8s 实战经验

```text
1. 从小开始,逐步规模化
2. 监控先行,日志其次
3. GitOps 是现代部署趋势
4. 多集群是企业级必需
5. 服务网格 (Istio) 是微服务未来
6. 灾备演练必须定期做
```

### 推荐技术栈

```text
- 安装: kubeadm 或托管 (EKS/GKE/ACK)
- 网络: Calico / Cilium
- 存储: Rook-Ceph / 云厂商块存储
- 入口: Nginx Ingress / Gateway API
- 服务发现: CoreDNS
- 配置: ConfigMap + Secret + External Secrets
- Helm: 应用打包
- 监控: Prometheus + Grafana
- 日志: Loki + Promtail
- 链路: Jaeger / Tempo
- GitOps: ArgoCD / Flux
- CI/CD: GitLab CI / Jenkins / GitHub Actions
- 服务网格: Istio / Linkerd (按需)
- 多集群: Rancher / Cluster API
```

### 选型决策

```text
单集群小规模 → kubeadm + Nginx Ingress + Loki
单集群中规模 → kubeadm + 多 Master + Prometheus + EFK
单集群大规模 → 托管 K8s (EKS/GKE) + Istio + 完整监控
多集群企业 → 托管 K8s + Rancher + ArgoCD + 多区域灾备
```

### 落地建议

```text
1. 不要一开始就用所有高级特性
2. 先解决 80% 的基础场景
3. 监控、日志、安全先做
4. CI/CD 流程打通
5. 性能优化按需
6. 渐进式引入服务网格
```

---

## 参考

- **K8s 实战案例**: https://kubernetes.io/docs/tutorials/
- **Helm Charts**: https://artifacthub.io/
- **GitOps**: https://opengitops.dev/
- **ArgoCD**: https://argo-cd.readthedocs.io/
- **Istio**: https://istio.io/latest/docs/
