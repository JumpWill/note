# K8s 集群节点上限与扩展性 (Kubernetes Cluster Scaling Limits)

> 本文档系统讲解 K8s 集群的节点规模上限，影响扩展性的关键因素，以及大规模集群的最佳实践。

## 一、K8s 集群节点规模理论上限

### 1.1 K8s 官方支持规模

```text
Kubernetes 官方支持的最大规模（v1.30+）：

  ┌────────────────────────────────────────┐
  │  节点数     │  5,000 节点              │
  │  Pod 总数    │  150,000 Pod            │
  │  容器总数    │  300,000 容器          │
  │  每个节点 Pod│  100 Pod（默认）       │
  └────────────────────────────────────────┘

  注意：
    - 这是 K8s 官方测试规模
    - 超过需要特殊优化
    - 部分组件有更严格的限制
```

### 1.2 关键组件限制

```text
K8s 各组件的理论极限：

  ┌─────────────────┬──────────────────┐
  │  组件            │  默认上限          │
  ├─────────────────┼──────────────────┤
  │  Pod 数          │  150,000          │
  │  Service 数      │  10,000           │
  │  Namespace 数    │  10,000           │
  │  Node 数         │  5,000            │
  │  每个 Node Pod 数│  110（默认 100） │
  │  Endpoint 数     │  100,000          │
  │  ConfigMap 总大小│  1 MB             │
  │  Secret 总大小   │  1 MB             │
  │  CRD 对象数      │  无硬限           │
  └─────────────────┴──────────────────┘
```

### 1.3 各云厂商支持规模

```text
主流云厂商 K8s 服务支持规模：

  ┌─────────────────┬──────────────────┐
  │  服务            │  最大节点数        │
  ├─────────────────┼──────────────────┤
  │  AWS EKS         │  100 节点/集群     │
  │                  │  (可多个集群)      │
  │  GCP GKE         │  15,000 节点      │
  │                  │  (Standard tier)   │
  │  Azure AKS       │  5,000 节点       │
  │  阿里云 ACK      │  5,000 节点       │
  │  腾讯云 TKE      │  5,000 节点       │
  │  华为云 CCE      │  5,000 节点       │
  └─────────────────┴──────────────────┘
```

---

## 二、影响集群规模的四大瓶颈

### 2.1 四大瓶颈速记

```text
大规模 K8s 集群四大瓶颈：

  1. etcd（数据存储）
     - 单 KV 存储
     - 受限于网络带宽和磁盘 IO
     - 默认上限 8GB

  2. API Server（控制面）
     - 每次 watch/list 调用
     - 受限于内存和网络
     - 处理所有组件请求

  3. Controller Manager（控制器）
     - 监听全集群资源
     - 全量 reconcile 开销
     - 单点处理所有 CRD

  4. Scheduler（调度器）
     - 每次调度评估所有节点
     - 默认并发 16
     - 大集群调度慢
```

### 2.2 瓶颈示意

```text
┌─────────────────────────────────────────────────────────────┐
│                    K8s 集群组件                              │
│                                                              │
│  ┌──────────────────────────────────────────────────┐    │
│  │              etcd（数据存储）                       │    │
│  │  - 所有集群状态都存在这里                          │    │
│  │  - 网络带宽瓶颈（频繁 watch）                     │    │
│  │  - 磁盘 IO 瓶颈（频繁写）                       │    │
│  │  - 单集群瓶颈                                      │    │
│  └──────────────────────────────────────────────────┘    │
│                            ↑                               │
│  ┌──────────────────────────────────────────────────┐    │
│  │              API Server                            │    │
│  │  - 所有组件的通信中枢                              │    │
│  │  - watch 请求放大（1000 节点 × N 控制器）       │    │
│  │  - 处理所有读写请求                                │    │
│  │  - 内存压力（List/Watch）                          │    │
│  └──────────────────────────────────────────────────┘    │
│                            ↑                               │
│  ┌──────────────────────────────────────────────────┐    │
│  │         Controller Manager                         │    │
│  │  - 多个控制器监听所有资源                          │    │
│  │  - 每个控制器处理全部 CRD                          │    │
│  │  - 单点处理所有 reconcile                          │    │
│  └──────────────────────────────────────────────────┘    │
│                            ↑                               │
│  ┌──────────────────────────────────────────────────┐    │
│  │              Scheduler                            │    │
│  │  - 每次调度评估所有节点                            │    │
│  │  - 默认并发数低                                    │    │
│  │  - 大集群调度慢                                    │    │
│  └──────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、etcd 性能与扩展

### 3.1 etcd 的关键限制

```text
etcd 默认限制（v3.5+）：

  ┌─────────────────┬──────────────────┐
  │  参数            │  默认值           │
  ├─────────────────┼──────────────────┤
  │  --quota-backend │  8 GB（默认）    │
  │  --max-request-bytes│ 1.5 MB        │
  │  --max-snapshot   │  8 MB 默认        │
  │  推荐最大 key    │  1.5 MB          │
  │  推荐最大 value  │  8 MB            │
  │  KV 总数          │  几百万（建议）   │
  │  watch 数        │  几千/客户端      │
  └─────────────────┴──────────────────┘

  K8s etcd 实际使用：
    - 1 个 Pod ≈ 1-2 KB
    - 1 个 Node ≈ 5-10 KB
    - 100,000 Pod ≈ 200 MB（安全）
    - 5,000 Node ≈ 50 MB（安全）
```

### 3.2 etcd 性能基准

```text
etcd 性能（典型 SSD、3 节点集群）：

  ┌─────────────────┬──────────────────┐
  │  操作            │  性能             │
  ├─────────────────┼──────────────────┤
  │  写             │  10,000 ops/s     │
  │  读             │  30,000 ops/s     │
  │  批量写         │  100,000 ops/s    │
  │  watch 通知       │  5,000/s          │
  │  KV 总数         │  10M+            │
  │  存储大小        │  8 GB（默认）     │
  └─────────────────┴──────────────────┘

  K8s 中 etcd 实际负载：
    - 100 节点 × 100 Pod = 10,000 资源
    - 每次 reconcile = N 次 etcd 写
    - 100 节点 → ~5,000 etcd ops/s
    - 1,000 节点 → ~50,000 etcd ops/s
    - 5,000 节点 → ~250,000 etcd ops/s ⚠️ 超限
```

### 3.3 etcd 优化与扩展

```text
etcd 性能优化清单：

  ┌─────────────────┬──────────────────┐
  │  优化项          │  收益             │
  ├─────────────────┼──────────────────┤
  │  SSD 替代 HDD    │  10-100x IOPS     │
  │  专用服务器       │  隔离干扰         │
  │  调大 --quota-backend│  突破 8GB      │
  │  启用压缩        │  减少磁盘 IO      │
  │  --max-request-bytes│ 优化大对象      │
  └─────────────────┴──────────────────┘

etcd 扩展方案：
  1. K8s v1.28+: Konnectivity（替代 SSH tunnel）
     - API Server 到 kubelet 不走 etcd
     - 减少 etcd 压力

  2. etcd 分片（实验性）
     - K8s v1.29+ 探索中
     - 不同 CR 分片存储

  3. WatchCache 优化
     - API Server 内存缓存
     - 减少 etcd 读压力

  4. CRD 收敛
     - 不必要的 CR 少用
     - 自定义 CR 控制数量
```

---

## 四、API Server 性能

### 4.1 API Server 工作负载

```text
API Server 处理的所有请求：

  ┌────────────────────────────────────────────────┐
  │  请求类型            │  占比（典型）  │
  ├────────────────────────────────────────────────┤
  │  LIST                │  30-40%        │
  │  WATCH               │  30-40%        │
  │  GET                 │  15-20%        │
  │  CREATE              │  5-10%         │
  │  UPDATE              │  5-10%         │
  │  DELETE              │  2-5%          │
  └────────────────────────────────────────────────┘

  写操作（CREATE/UPDATE/DELETE）：
    - 走 Raft 共识 → etcd 写
    - 延迟较高（10-100ms）
    - 是 etcd 压力的主要来源

  读操作（LIST/WATCH/GET）：
    - 从 etcd 读
    - 或从 API Server 缓存读
    - 是 API Server 内存的主要消耗
```

### 4.2 API Server 性能瓶颈

```text
API Server 资源瓶颈：

  ┌────────────────────────────────────────────────┐
  │  资源            │  典型瓶颈          │
  ├────────────────────────────────────────────────┤
  │  内存            │  WatchCache 占用大 │
  │  CPU            │  序列化/反序列化   │
  │  网络            │  List/Watch 流     │
  │  磁盘            │  etcd 间接影响     │
  └────────────────────────────────────────────────┘

  WatchCache 详解：
    - API Server 在内存中缓存资源
    - 默认 100MB（可调）
    - 大集群推荐 1-2GB
    - 1000 节点 × 100 Pod = 100K 对象
    - 每个对象 ≈ 2-5 KB
    - 总占用：~200-500 MB
```

### 4.3 API Server 调优

```yaml
# kube-apiserver 推荐参数
apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
apiServer:
  extraArgs:
    # 并发请求数
    - --max-requests-inflight=2000
    - --max-mutating-requests-inflight=1000
    
    # WatchCache 大小（默认 100MB）
    - --watch-cache-sizes=kube_apiservice=200,apiextensions.k8s.io/CustomResourceDefinition=500,apps/Deployment=2000
    
    # GC 调优
    - --default-watch-cache-size=1000
    
    # 特性开关
    - --feature-gates=APIPriorityAndFairness=true
    
    # 日志
    - --log-level=info
```

---

## 五、Controller Manager 与 Scheduler

### 5.1 Controller Manager 瓶颈

```text
Controller Manager 默认瓶颈：

  ┌────────────────────────────────────────────────┐
  │  问题              │  说明                │
  ├────────────────────────────────────────────────┤
  │  单实例处理         │  所有控制器跑在一处  │
  │  Watch 数量        │  几千 watch 难以扩展 │
  │  Reconcile 开销    │  100 节点几秒级     │
  │  GC 频繁           │  大集群 GC 压力大   │
  │  leader election  │  单点失效            │
  └────────────────────────────────────────────────┘

  K8s v1.20+ Controller Manager：
    - 单实例可处理 ~200 节点
    - 超过 200 节点需要扩展
    - v1.28 引入 leader election 优化
```

### 5.2 Controller Manager 水平扩展方案

```text
传统 Controller Manager 模式：

  ┌────────────────────┐
  │  Controller Manager  │ ← 单点
  │  (leader)            │
  └────────────────────┘
           │
       监听所有 CRD
           │
       处理所有 reconcile

Controller Manager Sharding（实验性）：

  ┌────────────────────┐
  │  Controller Manager  │ ← Controller Manager 实例 1
  │  Shard 1            │   处理 CRD A
  └────────────────────┘
  ┌────────────────────┐
  │  Controller Manager  │ ← Controller Manager 实例 2
  │  Shard 2            │   处理 CRD B
  └────────────────────┘

  按 CRD 类型或命名空间分片
```

### 5.3 Scheduler 瓶颈

```text
Scheduler 性能瓶颈：

  ┌────────────────────────────────────────────────┐
  │  问题              │  数据                │
  ├────────────────────────────────────────────────┤
  │  默认并发          │  16                 │
  │  评估所有节点       │  100%              │
  │  100 节点调度     │  ~100ms             │
  │  1000 节点调度    │  ~1s                │
  │  5000 节点调度    │  ~5s ⚠️ 慢          │
  └────────────────────────────────────────────────┘

  K8s v1.23+ Scheduler Sharding：
    - 按命名空间分片
    - 多 Scheduler 实例
    - 提升调度并发

  K8s v1.28+ Scheduler Profiles：
    - 自定义调度策略
    - 多 Profile 并存
```

### 5.4 Scheduler 调优

```yaml
# kube-scheduler 推荐参数
apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
scheduler:
  extraArgs:
    # 并发度
    - --scheduler-name=default-scheduler
    - --feature-gates=SchedulerSharding=true
    
    # 调度性能
    - --percentage-of-nodes-to-score=50
      # 只评估 50% 节点（最优节点）
      # 大集群可降到 10-30%
    
    # 绑定超时
    - --bind-timeout=10s
```

---

## 六、Kubelet 与节点瓶颈

### 6.1 Kubelet 默认限制

```text
Kubelet 关键参数与默认：

  ┌─────────────────┬──────────────────┐
  │  参数            │  默认值            │
  ├─────────────────┼──────────────────┤
  │  --max-pods      │  110              │
  │  --node-status-update-frequency│ 10s│
  │  --image-gc-high-threshold│ 85%   │
  │  --image-gc-low-threshold│ 80%    │
  │  --kube-api-burst│  30             │
  │  --kube-api-qps   │  50             │
  └─────────────────┴──────────────────┘

  K8s v1.28+：
    --max-pods 可在 KubeletConfiguration 设置
    单节点推荐 100 Pod
    大内存节点可调到 250
```

### 6.2 单节点最大 Pod 数

```text
单节点 Pod 数计算公式：

  maxPods = min(
    --max-pods 配置,
    节点 CPU / 单 Pod CPU 需求,
    节点内存 / 单 Pod 内存需求,
    节点 PID 数 / 单 Pod PID 需求,
    节点 IP 数 / Pod 数,
  )

  示例：
    节点：16 核 64GB
    Pod：1 核 2GB
    理论：min(110, 16, 32, ...) = 16 个
    实际：约 16-20 个 Pod

  K8s 默认 --max-pods=110
    - 通常不是 CPU/内存限制
    - 是 IP/PID/conntrack 限制
    - 实际部署要综合考虑
```

### 6.3 Kubelet 调优

```yaml
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
maxPods: 250                          # 提高单节点 Pod 数
nodeStatusUpdateFrequency: 30s        # 减少 API Server 压力
imageGCHighThresholdPercent: 85
imageGCLowThresholdPercent: 80
# kube-api-qps 调整
kubeAPIQPS: 100                       # 默认 50
kubeAPIBurst: 200                     # 默认 100
# cgroup 配置
cgroupDriver: systemd
# 启用镜像预加载
serializeImagePulls: false
# 镜像拉取超时
imagePullProgressDeadline: 5m
```

---

## 七、网络瓶颈

### 7.1 K8s 网络组件限制

```text
K8s 网络瓶颈（控制面）：

  ┌─────────────────┬──────────────────┐
  │  组件            │  默认限制          │
  ├─────────────────┼──────────────────┤
  │  Service 数量    │  10,000          │
  │  Endpoint 数     │  100,000         │
  │  ClusterIP 总数  │  受限于 Service  │
  │  iptables 规则   │  ~5,000 影响性能 │
  │  conntrack 表   │  受限于内存      │
  └─────────────────┴──────────────────┘

  Service 跨节点通信：
    - 默认通过 kube-proxy iptables
    - 1000 Service = 大量 iptables 规则
    - 影响网络性能
    - 推荐用 IPVS 或 Cilium eBPF
```

### 7.2 Endpoint 数量瓶颈

```text
Endpoint 数量影响：

  1. 每个 Service 后端 Pod 变化
     → Endpoint 对象更新
     → 触发 kube-proxy reload
     → 更新 iptables/IPVS 规则

  2. 大 Service 数量
     → 大量 Endpoint 对象
     → API Server 内存占用大
     → kube-proxy reload 频繁

  优化：
    - 使用 EndpointSlice（K8s v1.21+ GA）
      把 Endpoint 拆分，减少单个对象大小
    - 使用 Cilium 替代 kube-proxy
```

### 7.3 网络策略扩展性

```text
NetworkPolicy 数量与性能：

  ┌─────────────────┬──────────────────┐
  │  Policy 数      │  iptables 性能影响  │
  ├─────────────────┼──────────────────┤
  │  < 100          │  几乎无影响        │
  │  100-1000       │  性能下降 < 5%    │
  │  1000-5000      │  性能下降 10-20%  │
  │  > 5000         │  性能下降明显     │
  │  > 10000        │  需要 eBPF 数据面  │
  └─────────────────┴──────────────────┘

  解决：
    - 使用 Cilium eBPF（10,000+ Policy 仍流畅）
    - 使用 IPVS 替代 iptables
    - 合并 Policy（减少规则数）
```

---

## 八、大规模集群最佳实践

### 8.1 大规模集群架构

```text
大规模 K8s 集群推荐架构：

  ┌─────────────────────────────────────────────────────┐
  │                    控制面                            │
  │                                                      │
  │  ┌──────────────────────────────────────────────┐  │
  │  │            etcd 集群（3 或 5 节点）           │  │
  │  │  - 专用 SSD 服务器                          │  │
  │  │  - 10GbE 网络                               │  │
  │  │  - 低延迟                                   │  │
  │  └──────────────────────────────────────────────┘  │
  │                       ↑                              │
  │  ┌──────────────────────────────────────────────┐  │
  │  │  API Server × 3（高可用）                   │  │
  │  │  - 16 核 32GB+                              │  │
  │  │  - 负载均衡                                  │  │
  │  └──────────────────────────────────────────────┘  │
  │                       ↑                              │
  │  ┌──────────────────────────────────────────────┐  │
  │  │  Controller Manager + Scheduler              │  │
  │  │  - HA（多实例）                              │  │
  │  │  - Scheduler Sharding                        │  │
  │  └──────────────────────────────────────────────┘  │
  └─────────────────────────────────────────────────────┘
                          ↑
                  ┌───────┴───────┐
                  ↓               ↓
            Worker 节点 × N   Worker 节点 × N
            （业务 Pod）     （业务 Pod）
```

### 8.2 etcd 部署最佳实践

```text
etcd 集群部署（生产推荐）：

  ┌─────────────┬─────────────┬─────────────┐
  │  etcd-1     │  etcd-2     │  etcd-3     │
  │  (Leader)   │  (Follower) │  (Follower) │
  │  10.0.0.11  │  10.0.0.12  │  10.0.0.13  │
  └─────────────┴─────────────┴─────────────┘
        ↑              ↑             ↑
        └──────── SSD 存储 ────────┘

  推荐配置：
    - 3 节点（容忍 1 节点故障）
    - 5 节点（容忍 2 节点故障）
    - 每节点：8 核 16GB SSD
    - 10GbE 网络互联

  K8s v1.28+ 新特性：
    - Konnectivity：API Server 与 kubelet 走 HTTPS
    - 不再依赖 SSH tunnel
    - 减少 etcd 网络负载
```

### 8.3 K8s 大规模集群配置

```yaml
# kubeadm 配置大规模集群
apiVersion: kubeadm.k8s.io/v1beta3
kind: InitConfiguration
localAPIEndpoint:
  advertiseAddress: 10.0.0.10
  bindPort: 6443
nodeRegistration:
  criSocket: unix:///run/containerd/containerd.sock

---
apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: v1.30.0
etcd:
  local:
    extraArgs:
      quota-backend-bytes: "8589934592"     # 8GB
      max-request-bytes: "33554432"          # 32MB
      election-timeout: "5000"
      heartbeat-interval: "500"
    serverCertSANs:
      - 10.0.0.10

apiServer:
  extraArgs:
    - --max-requests-inflight=2000
    - --max-mutating-requests-inflight=1000
    - --default-watch-cache-size=1000
    - --watch-cache-sizes=apps/Deployment=2000,core/Endpoints=5000
    - --feature-gates=APIPriorityAndFairness=true,WatcherConcurrency=true
  timeoutForControlPlane: 4m0s

controllerManager:
  extraArgs:
    - --node-monitor-period=5s
    - --node-monitor-grace-period=40s
    - --pod-eviction-timeout=5m
    - --feature-gates=ControllerManagerSharding=true

scheduler:
  extraArgs:
    - --feature-gates=SchedulerSharding=true
    - --percentage-of-nodes-to-score=33
```

### 8.4 大规模集群监控

```text
大规模集群必须监控的关键指标：

  ┌─────────────────┬──────────────────┐
  │  类别            │  指标              │
  ├─────────────────┼──────────────────┤
  │  etcd           │  - db size         │
  │                 │  - write QPS      │
  │                 │  - disk fsync 延迟│
  │                 │  - leader 切换次数 │
  │  API Server     │  - 请求延迟        │
  │                 │  - goroutine 数   │
  │                 │  - 内存使用        │
  │                 │  - watch 数        │
  │  Scheduler      │  - 调度延迟        │
  │                 │  - 调度队列长度    │
  │  Kubelet        │  - Pod 启动延迟   │
  │                 │  - 镜像 GC 频率   │
  │  网络           │  - iptables 规则数 │
  │                 │  - conntrack 表大小│
  └─────────────────┴──────────────────┘
```

---

## 九、K8s v1.30+ 新特性（解决扩展性）

### 9.1 Konnectivity（替代 SSH tunnel）

```text
K8s v1.20+ Konnectivity 架构：

  传统架构（v1.20 前）：
    API Server ←─SSH tunnel──→ kubelet
    （每个节点 SSH 连接）

  Konnectivity 架构（v1.20+）：
    API Server ←─HTTPS──→ Konnectivity Server ←─→ kubelet
                   ↑
            （复用一条 HTTPS 连接）
```

### 9.2 WatcherConcurrency

```text
  K8s v1.20+ WatcherConcurrency：
  
    API Server 默认一个 WATCH 流
    WatcherConcurrency 启用多流
    减少 LIST/WATCH 时的资源占用
```

### 9.3 APIPriorityAndFairness

```text
APIPriorityAndFairness（K8s v1.18+ GA）：

  默认行为：
    - 所有请求公平排队
    - 突发流量可能导致正常请求排队

  启用 APF 后：
    ┌─────────────────┬─────────────┐
    │  Priority Level  │  配额        │
    ├─────────────────┼─────────────┤
    │  system          │  最高        │
    │  leader-election  │  高          │
    │  workload-high   │  中          │
    │  workload-low    │  低          │
    │  global-default  │  默认（兜底）│
    └─────────────────┴─────────────┘

  启用：
    --feature-gates=APIPriorityAndFairness=true

  避免某个 controller 占用所有资源
```

### 9.4 SchedulerSharding

```text
SchedulerSharding（K8s v1.23+）：

  传统模式：
    单 Scheduler 处理所有命名空间

  Sharding 模式：
    ┌──────────────────┐    ┌──────────────────┐
    │  Scheduler-1      │    │  Scheduler-2      │
    │  ns: ns-a, ns-b   │    │  ns: ns-c, ns-d   │
    └──────────────────┘    └──────────────────┘

  启用：
    --feature-gates=SchedulerSharding=true

  优势：
    - 调度并行度提升 N 倍
    - 大集群调度延迟降低
```

### 9.5 ClusterResolver for kubelet

```text
K8s v1.28+ ClusterResolver：

  问题：
    - kubelet 直连 API Server
    - 5000 节点 × 频繁心跳 = 网络压力大

  解决：
    - kubelet 通过 Konnectivity 连 API Server
    - 复用一条 HTTPS 长连接
    - 减少 ~80% 网络流量
```

---

## 十、K8s 多集群方案

### 10.1 为什么需要多集群

```text
单集群瓶颈：

  ┌────────────────────────────────────────┐
  │  单集群上限                        │
  ├────────────────────────────────────────┤
  │  节点：5000                      │
  │  Pod：150000                     │
  │  Service：10000                  │
  │  Namespace：10000                │
  └────────────────────────────────────────┘

  突破方式：多集群

  多集群方案：
    1. 单集群多 Federation（已废弃）
    2. Cluster API（推荐）
    3. 跨集群 Service Mesh（Istio）
    4. 多集群 Cilium Cluster Mesh
    5. 各云厂商多集群方案
```

### 10.2 Cluster API（CAPI）

```text
Cluster API 架构：

  ┌──────────────────────────────────────────────┐
  │            Management Cluster                    │
  │  ┌────────────────┐  ┌────────────────┐   │
  │  │ cluster-api    │  │ provider      │   │
  │  │ controller     │  │ controllers    │   │
  │  └────────────────┘  └────────────────┘   │
  └────────────┬─────────────────────────────────┘
               │ 创建/管理
               ↓
  ┌──────────────────────────────────────────────┐
  │            Workload Cluster                    │
  │  ┌─────────────────────────────────────────┐ │
  │  │  CAPI 创建的 K8s 集群                  │ │
  │  │  节点：500-2000（中等规模）            │ │
  │  └─────────────────────────────────────────┘ │
  └──────────────────────────────────────────────┘
```

### 10.3 Cilium Cluster Mesh

```text
Cilium Cluster Mesh：

  ┌──────────────┐         ┌──────────────┐
  │  Cluster A   │         │  Cluster B   │
  │  Cilium       │◄────────►│  Cilium       │
  │  10.0.0.0/16  │ Cluster │  10.1.0.0/16  │
  │              │ Mesh    │              │
  └──────────────┘         └──────────────┘
         │                        │
         └── Pod A ──────────────┘
              可跨集群通信

  特性：
    - 跨集群 Pod-to-Pod 通信
    - 跨集群 NetworkPolicy
    - 跨集群 Service Discovery
    - 全局可观测性
```

---

## 十一、核心要点速记

### 集群规模速记

```text
理论上限：
  K8s v1.30+：5000 节点 / 150000 Pod
  etcd 数据：8 GB 默认
  
云厂商支持：
  EKS：100 节点/集群
  GKE：15000 节点
  AKS/ACK：5000 节点
```

### 四大瓶颈速记

```text
1. etcd：单 KV、网络/磁盘
2. API Server：内存/watch
3. Controller Manager：单实例
4. Scheduler：调度评估
```

### 大规模优化速记

```text
etcd 优化：
  SSD、专用服务器、调大 quota
  
API Server 优化：
  --max-requests-inflight=2000
  --default-watch-cache-size=1000
  APIPriorityAndFairness=true
  
Controller Manager 优化：
  ControllerManagerSharding
  
Scheduler 优化：
  --percentage-of-nodes-to-score=33
  SchedulerSharding=true
  
网络优化：
  EndpointSlice 替代 Endpoint
  Cilium eBPF 替代 iptables
```

### K8s v1.30+ 新特性

```text
Konnectivity：HTTPS 替代 SSH tunnel
WatcherConcurrency：多 watch 流
APIPriorityAndFairness：请求优先级
SchedulerSharding：多 Scheduler 实例
ClusterResolver：减少 kubelet 连接
```

### 多集群方案

```text
单集群瓶颈（5000 节点）→ 多集群
方案：
  Cluster API（CAPI）
  Cilium Cluster Mesh
  Service Mesh 多集群
  各云厂商多集群方案
```

### 一句话总结

```text
K8s 单集群上限：5000 节点
四大瓶颈：etcd / API Server / CM / Scheduler
突破方式：
  - 短期：调优（etcd SSD / API Server QPS）
  - 中期：新特性（Konnectivity / Sharding / APF）
  - 长期：多集群（CAPI / Cluster Mesh）
```

### 关键调优速记

```text
etcd：
  SSD、3 节点、--quota-backend-bytes=8G

API Server：
  --max-requests-inflight=2000
  --default-watch-cache-size=1000

Controller：
  Sharding=true
  
Scheduler：
  --percentage-of-nodes-to-score=33
  
Kubelet：
  --max-pods=250
  --kube-api-qps=100
  
网络：
  Cilium eBPF（替代 iptables）
  EndpointSlice（替代 Endpoint）
```

---

## 十二、参考资源

```text
- K8s 大规模集群：https://kubernetes.io/docs/setup/best-practices/cluster-large/
- K8s 组件指标：https://kubernetes.io/docs/concepts/cluster-administration/system-metrics/
- etcd 性能：https://etcd.io/docs/v3.5/op-guide/performance/
- Konnectivity：https://kubernetes.io/docs/concepts/architecture/control-plane-node-interaction/
- APIPriorityAndFairness：https://kubernetes.io/docs/concepts/cluster-administration/priority-fairness/
- Cluster API：https://cluster-api.sigs.k8s.io/
- Cilium Cluster Mesh：https://docs.cilium.io/en/stable/network/clustermesh/clustermesh/
- Kubelet 配置：https://kubernetes.io/docs/reference/config-api/kubelet-config.v1beta1/
- 调度器配置：https://kubernetes.io/docs/reference/scheduling/config/
```

## 快速参考卡

```
K8s 单集群：
  5000 节点 / 150000 Pod（官方测试规模）
  实际生产推荐：1000-2000 节点

四大瓶颈：
  1. etcd（数据/网络）
  2. API Server（内存/watch）
  3. Controller Manager（单点）
  4. Scheduler（调度评估）

大规模集群必开特性：
  Konnectivity（替代 SSH）
  APIPriorityAndFairness
  SchedulerSharding
  WatcherConcurrency

etcd 调优：
  SSD、专用服务器、quota 8GB

API Server 调优：
  max-requests-inflight=2000
  default-watch-cache-size=1000

网络替代：
  iptables → IPVS（K8s 原生）
  iptables → eBPF（Cilium）
  Endpoint → EndpointSlice
```
